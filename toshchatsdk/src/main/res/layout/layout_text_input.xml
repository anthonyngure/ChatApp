<?xml version="1.0" encoding="utf-8"?><!--
  ~ Copyright (c) 2016. VibeCampo Social Network
  ~
  ~ Website : http://www.vibecampo.com

  -->

<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:vibecampo="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:gravity="bottom"
    android:layout_height="wrap_content">

    <ImageButton
        style="@style/Widget.AppCompat.ImageButton"
        android:background="?attr/selectableItemBackground"
        android:id="@+id/sendButton"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:tint="@color/colorAccent"
        android:src="@drawable/ic_send_black_24dp"
        android:layout_alignBottom="@+id/textInput"
        android:layout_alignParentEnd="true"
        android:layout_alignParentRight="true"
        android:layout_margin="5dp"
        android:scaleType="centerInside"
        android:visibility="gone" />

    <LinearLayout
        android:id="@+id/textInput"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_toLeftOf="@+id/sendButton"
        android:orientation="horizontal"
        android:layout_toStartOf="@+id/sendButton"
        android:layout_alignParentStart="true">

        <LinearLayout
            android:layout_width="@dimen/vibecampo_round_button"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom"
            android:orientation="vertical">

            <ImageButton
                android:id="@+id/emojiButton"
                android:layout_width="@dimen/vibecampo_round_button"
                android:layout_height="@dimen/vibecampo_round_button"
                android:layout_marginBottom="3dp"
                android:background="@drawable/vibecampo_round_button"
                android:src="@drawable/ic_insert_emoticon_black_24dp"
                android:tint="@color/colorPrimary" />

            <View
                android:id="@+id/counterMargin"
                android:layout_width="match_parent"
                android:layout_height="20dp"
                android:visibility="gone" />
        </LinearLayout>

        <com.vanniktech.emoji.EmojiEditText
            android:id="@+id/emojiEditText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:imeOptions="actionSend"
            android:inputType="textCapSentences|textMultiLine"
            android:maxLines="3"
            android:fadeScrollbars="false"
            android:gravity="bottom"
            android:scrollbarSize="2dp"
            android:scrollbarThumbVertical="@color/colorAccent"
            android:scrollbars="vertical"
            android:scrollbarStyle="outsideOverlay"
            android:verticalScrollbarPosition="right"/>

    </LinearLayout>
</RelativeLayout>